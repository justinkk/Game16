using UnityEngine;
using System.Collections;

public class MinigameManager2 : MinigameManager {
    override public void UpdateEvent(int index, string e) {
        if (string.Compare(e, "Bonus") == 0)
        {
        }
    }

}
